sudo: true
language: java
jdk:
  - openjdk11
  
before_install:
  gradle wrapper

branches:
  only:
    - master

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "gradle clean build -x test"
